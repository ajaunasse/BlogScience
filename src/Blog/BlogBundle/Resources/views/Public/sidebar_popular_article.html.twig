<div class="tab-pane  fade active in" id="popular-posts">
	<div class="push-down-15">
		<img src="{{ asset('uploads/images/') ~ popArticle.getImage().getPath() }}" alt="{{ popArticle.getImage().getName() }}"  width="90" height="60"/>
		<h5>
			<a href="single-post.html">{{ popArticle.titre }}</a> ({{ popArticle.nbVues }} vues)
		</h5>
		<span class="widget-posts__time">{{ popArticle.date|date('d M Y') }}</span>
	</div>
</div>